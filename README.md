# AnimeDL

Десктопное приложение для поиска и скачивания аниме с русскоязычных онлайн-кинотеатров.

## Как устроено сейчас (as-is)

Архитектура — GUI-обёртка (Qt/C++) вокруг двух независимых консольных инструментов,
общение с которыми идёт через `QProcess` + JSON в stdout.

```
┌─────────────────────────┐
│   Qt6 Widgets (C++)     │  mainwindow.cpp/.ui, logwindow.cpp/.ui
│   MainWindow            │  вся логика в слотах on_*_clicked/on_*_itemClicked
└───────────┬──────────────┘
            │ QProcess, argv, stdout(JSON)/stderr, таймаут 20 сек
            ▼
┌─────────────────────────┐      ┌──────────────────────────┐
│  backend (PyInstaller)  │      │  yt-dlp (отдельный exe)  │
│  Python + anicli_api    │      │  скачивание .mp4/.m3u8   │
└───────────┬──────────────┘      └──────────────────────────┘
            │
            ▼
   anilibria / animego / animevost / dreamcast /
   sameband / yummy_anime / yummy_anime_org
   (жёстко зашитые Extractor-классы из anicli-ru)
```

### Frontend (Qt/C++)

- `main.cpp`, `mainwindow.cpp/.h/.ui` — главное окно: строка поиска, список источника
  (провайдер), список аниме, списки серий (одна / несколько), список озвучек/плееров,
  список качеств видео, поле пути сохранения, настройки SOCKS5-прокси, список
  запущенных процессов скачивания.
- `logwindow.cpp/.h/.ui` — отдельное окно с вкладками, по вкладке на каждый процесс
  `yt-dlp` (stdout/stderr в реальном времени).
- Настройки (прокси, путь скачивания) хранятся через `QSettings` (реестр/plist/ini).
- На каждое действие пользователя (поиск, выбор аниме, выбор серии, выбор источника)
  заново перезапускается `backend.exe` с полным набором аргументов и **пересчитывает
  весь путь с нуля** — состояние на стороне бэкенда не хранится (это CLI, не сервис).

### Backend (`backend/backend.py`)

Тонкая обёртка над библиотекой [anicli-ru (anicli_api)](https://github.com/vypivshiy/ani-cli-ru):
принимает аргументы командной строки (`search|episodes|sources|video_qualities|videos_urls`,
опционально `proxy <ip> <port> <user> <pass>` первым блоком) и печатает JSON-массив в stdout.

`get_extractor()` — `match/case` по имени провайдера, вручную сопоставленному с одним из
7 классов `Extractor` библиотеки anicli_api. Список провайдеров задан в двух местах
одновременно и независимо: в `comboBoxProvider` (mainwindow.ui) и в `match/case`
(backend.py) — источники нельзя добавить/убрать/настроить без пересборки обеих частей.

Собирается через PyInstaller (`--onedir`) в `backend/dist/backend/`, при сборке CMake
копирует эту директорию рядом с `animedl.exe`.

### Скачивание — yt-dlp

`yt-dlp` скачивается автоматически в CMake-скрипте (последний релиз с GitHub), запускается
как независимый `QProcess` на каждую серию, с ограничением на число параллельных
процессов (`spinBoxProcesses`) — реализовано вручную очередью групп
(`startNextGroup`/`pendingEpisodes`) в `mainwindow.cpp`. Прогресс и лог каждого процесса
уходят в `LogWindow`.

### Сборка

`CMakeLists.txt` — оркестрирует всё: собирает Qt-приложение, создаёт venv и ставит
зависимости backend'а (`bootstrap_backend.py`), пересобирает его PyInstaller'ом только
при изменении хэша исходников (`build_backend.py`), скачивает `yt-dlp`, копирует
`version.txt`. CI (`.github/workflows/build.yml`) собирает релиз только под Windows
(MinGW + `windeployqt`).

### Ограничения текущей архитектуры

- Источники (провайдеры) жёстко зашиты в коде на двух языках сразу — добавить свой
  источник или отключить сломанный без пересборки нельзя.
- Нет общего состояния сессии — каждый клик пересчитывает всю цепочку поиска заново
  через новый процесс Python.
- Смешение двух рантаймов (C++/Qt + Python/PyInstaller) усложняет сборку и упаковку
  (venv, хэш пересборки, PyInstaller, windeployqt).
- Кросс-платформенность декларативная (`Q_OS_WIN`, `#else`), но CI и релизы — только
  Windows.
- Индикация процессов скачивания хранит сырой указатель `QProcess*` в
  `QListWidgetItem::UserRole` (`quintptr`) — работает, но хрупко.
- Только SOCKS5-прокси, один общий тумблер на всё приложение.

## План: переезд на Node.js + Electron

Цель — оставить пользовательский сценарий тем же (поиск → выбор аниме → выбор серий →
выбор озвучки/источника → выбор качества → скачивание через yt-dlp, с логами и
прокси-настройками), но:

1. убрать двухъязычную архитектуру (C++/Qt + Python) в пользу единого рантайма
   Node.js/TypeScript;
2. отвязать парсинг источников от жёстко зашитого списка — сделать источники
   плагинами с единым интерфейсом, которые можно включать/выключать/добавлять из
   самого приложения (без пересборки);
3. десктоп-оболочка — **Electron** (зафиксировано): полный Node.js в main-процессе
   (не нужен отдельный sidecar-процесс для логики, как потребовалось бы в Tauri),
   IPC между main и renderer, богатая экосистема (`electron-builder`,
   `electron-store`, `electron-updater`), одинаково собирается под
   Windows/macOS/Linux — в отличие от текущего Qt-проекта, релизящегося только под
   Windows.

### Целевая архитектура

```
┌───────────────────────────────┐
│  Renderer (React + TS)         │  тот же набор экранов, что и сейчас
└───────────────┬────────────────┘
                │ Electron IPC (contextBridge/preload)
                ▼
┌───────────────────────────────┐
│  Main process (Node.js/TS)     │
│  - Source Registry (плагины)   │
│  - Download Manager (yt-dlp)   │
│  - Settings (electron-store)   │
└──────┬───────────────┬─────────┘
       │               │
       ▼               ▼
┌─────────────┐  ┌───────────────────┐
│ source-*    │  │ yt-dlp (бинарник /  │
│ плагины     │  │ yt-dlp-wrap/        │
│ (по одному  │  │ youtube-dl-exec)    │
│ на сайт)    │  └───────────────────┘
└─────────────┘
```

- **Source Registry** — единый интерфейс плагина источника
  (`search(query)`, `getEpisodes(animeId)`, `getSources(episodeId)`,
  `getQualities(sourceId)`), реестр загружает плагины из папки конфигурации
  пользователя (`~/.config/animedl/sources/*` или `%APPDATA%`), а не из
  захардкоженного `match/case`. Экран настроек показывает список источников с
  тумблерами вкл/выкл и возможностью добавить новый (файл плагина / URL).
- Логика поиска/эпизодов/качеств — портируется 1:1 по смыслу из `backend.py`
  (`search_anime`, `get_episodes`, `get_sources`, `get_video_qualities`,
  `get_videos_urls`), но как чистые TS-функции, без перезапуска процесса на каждый шаг.
- Скачивание — через `yt-dlp` (бинарник, как и сейчас) или `yt-dlp-wrap`/`youtube-dl-exec`
  из Node; очередь с ограничением параллельности — явный класс `DownloadQueue`, а не
  ad hoc список указателей на процессы.
- Прокси — `socks-proxy-agent`/встроенная поддержка `--proxy` в yt-dlp, тот же UX
  (SOCKS5, вкл/выкл, поля ip/port/user/pass).
- Настройки — `electron-store` вместо `QSettings`.
- Логи процессов — тот же UX (окно с вкладками по процессу), на базе `EventEmitter`
  из `DownloadManager` и React-компонента с вкладками.

### Стек

- Оболочка: **Electron**.
- UI: **React** + TypeScript, сборщик **Vite**.
- Ядро (main-процесс): Node.js + TypeScript.

## Список задач: от текущего состояния до целевого

Задачи идут в порядке выполнения — каждый следующий раздел опирается на предыдущий.
Qt/Python-код не трогаем до самого последнего пункта, чтобы всегда была рабочая
версия под рукой.

### 0. Подготовка — ✅ сделано
- [x] Выбрать оболочку — **Electron**.
- [x] Выбрать UI-фреймворк — **React** (+ TypeScript, Vite).

### 1. Каркас проекта — ✅ сделано
- [x] Старый Qt/Python-код перенесён в `old/`, не трогаем его до раздела 9.
- [x] Настроен Electron-проект: `electron/main.ts`, `electron/preload.ts`,
      `src/` (React + Vite), TypeScript (`tsconfig.app.json`/`tsconfig.node.json`),
      ESLint (flat config) + Prettier, npm.
- [x] Настроен запуск в dev-режиме (`npm run dev`, HMR renderer + hot-restart main
      через `vite-plugin-electron`) — проверено живым запуском.
- [x] Настроен тестовый прогон `npm run build` (`tsc -b && vite build`) без ошибок.
- [x] *(добавлено по ходу работы)* Настроен `vitest` + `@testing-library/react`
      (`npm run test`) — сейчас 2 файла тестов: `src/App.test.tsx` (полный
      пользовательский сценарий кликами: поиск → выбор тайтла → эпизод →
      источник → качество → выбрать серии → скачать → job во вкладке
      "Загрузки") и `src/EpisodeBrowser.test.tsx` (выбор серий кликом и
      протягиванием мыши — одиночный клик, диапазон в обе стороны, замена
      выбора новым кликом, сам процесс скачивания). Найден и исправлен реальный
      баг test-инфраструктуры по пути: без `test.globals` в vitest автоочистка
      DOM `@testing-library/react` между тестами не включается сама (нужен явный
      `afterEach(cleanup)` в `test-setup.ts`) — без него тесты в одном файле
      видят DOM друг друга и падают нестабильно (проверено: без фикса 1 из 3
      прогонов падал, 5 из 5 после фикса — зелёные).
      Юнит-тестов на чистую логику ядра (`SourceRegistry`, `withTimeout`,
      `DownloadQueue`) пока нет — задел на будущее.

### 2. Ядро: источники (Source Registry)
- [x] Определён общий интерфейс плагина источника (`AnimeSourcePlugin` в
      `shared/sourcePlugin.ts`): `search`, `getEpisodes`, `getSources`,
      `getQualities`, `getVideoUrls`, плюс типы результатов в
      `shared/animeTypes.ts`.
- [x] Реализован `SourceRegistry` (`electron/sources/registry.ts`) и IPC-слой
      (`electron/sources/ipc.ts`): источники включаются/выключаются через
      JSON-конфиг (`sources.json` в userData, редактируется в UI), таймаут поиска
      теперь настраивается на источник (было зашито 20 сек на всё приложение).
      Загрузка сторонних плагинов из папки пользователя — пока не реализована,
      это отдельная задача в разделе 6.
- [ ] Портировать реальную логику парсинга для каждого из 6 источников (anilibria,
      animego, animevost, dreamcast, sameband, yummy_anime — было 7, `yummy_anime_org`
      убран из `DEFAULT_SOURCES`, см. отдельный пункт ниже). Архитектура (IPC,
      таймауты, конфиг) проверена end-to-end ещё на заглушках; теперь
      портируется каждый источник по отдельности (4 из 6 готовы: anilibria,
      animego, yummy_anime, dreamcast):
  - [x] anilibria (`electron/sources/anilibria.ts`) — портирован полностью,
        через официальный публичный JSON API (без HTML-скрейпинга). Проверено
        вживую реальными запросами: поиск, список эпизодов (12/12), качества
        (480/720 m3u8), ссылки на несколько эпизодов сразу, и что `yt-dlp`
        реально распознаёт полученный m3u8-манифест
        (`--print "%(protocol)s"` → `m3u8_native`). Особенности, полезные для
        портирования остальных источников:
        - Домен и API исторически путаются в описаниях `anicli_api`: сайт —
          `anilibria.top`, но сам API отдаётся с `aniliberty.top/api/v1`
          (проверено — оба хоста реальны, работает именно второй). Старый
          `api.anilibria.tv/v2` из официального репо `anilibria/docs` — это
          **предыдущая** версия API, актуальная библиотека её уже не использует.
        - Точную схему эндпоинтов проще всего смотреть не в HTML-странице
          Swagger UI (она SPA, рендерится JS), а забрать её JSON-спеку напрямую:
          `/storage/api/docs/v1?aniliberty-api-v1-docs.json` (путь нашёлся через
          `<script src>` в исходнике страницы `/api/docs/v1`).
        - Ответ API может быть gzip-сжат — `curl` без `--compressed` вернёт
          пустое тело без ошибки, это легко принять за неработающий эндпоинт.
        - У AniLibria только одна собственная озвучка на релиз (в отличие от
          агрегаторов вроде AnimeGo) — шаг "выбор источника/озвучки"
          (`getSources`) здесь фиктивный, всегда один пункт.
        - Прокси для поиска (SOCKS5) в `SourceRequestOptions.proxy` пока
          архитектурно прокинут, но не применяется к `fetch()` — Node/undici не
          поддерживает SOCKS-прокси "из коробки", для этого нужен
          `socks-proxy-agent` (или аналог) с `dispatcher`, а проверить его не на
          чем (нет доступного SOCKS5-сервера под рукой). Сделано осознанно
          отдельной задачей ниже, а не наугад.
  - [x] animego (`electron/sources/animego.ts`) — **практически полностью
        портирован**, все 5 методов проверены вживую реальными запросами
        (поиск «One Punch Man» → 12/12 эпизодов у Ванпанчмен 3 → 21 реальная
        комбинация озвучка+плеер → реальные ссылки на видео через Kodik и
        AniBoom, проверенные `yt-dlp`). Единственное неподдержанное —
        собственный cdn-iframe animego.me (не Kodik и не AniBoom): для него
        `getQualities`/`getVideoUrls` кидают понятную ошибку с именем
        неподдержанного плеера, а не тихий отказ или (хуже) угадывание.
        Особенности, полезные при портировании остальных источников:
        - У сайта нет публичного API — список серий и озвучек подгружаются
          отдельными недокументированными AJAX-запросами
          (`/player/{id}/episodes`, `/player/videos/{episodeId}`), которые
          нашлись через разбор минифицированного JS-бандла сайта
          (`app.player_episodes...js`, метод `selectEpisode` → `GET
          /player/videos/${episodeId}`), а не по описанию в `anicli_api`.
        - `yt-dlp` не умеет скачивать ни Kodik (`kodikplayer.com`), ни
          AniBoom (`aniboom.one`) напрямую (проверено —
          `--list-extractors` не находит ни то, ни другое) — для реального
          скачивания через оба плеера нужен свой экстрактор, это не
          "подключить готовое".
  - [x] yummy_anime (`electron/sources/yummyAnime.ts`) — портирован полностью,
        кроме плеера Alloha (см. ниже). Проверено вживую реальными запросами:
        поиск «Наруто», 220/220 эпизодов, 8 комбинаций озвучка+плеер на серию,
        реальное видео и через Kodik, и через новый плеер SibNet. Особенности:
        - Реальный API живёт на отдельном домене `api.yani.tv`, а не на
          `site.yummyani.me` — нашлось только по разбору исходников
          `anicli_api`, на самом сайте нигде не описано. Чистый JSON REST, без
          HTML-скрейпинга.
        - Эпизоды приходят одним плоским списком видео на весь тайтл (1793
          штуки у Наруто разом) без кэширования на сервере по сериям — их
          нужно самостоятельно группировать по полю `number`.
        - Alloha (примерно 30% видео у yummy_anime) сознательно не
          реализован — сам `anicli_api` тоже его пропускает как "too complex
          reverse", и здесь то же решение, а не недосмотр.
  - [x] dreamcast (`electron/sources/dreamcast.ts`) — портирован полностью:
        поиск/эпизоды/качества проверены вживую (поиск «Re:Zero» → 16/16
        эпизодов текущего 4 сезона → HLS-манифест, подтверждённый `yt-dlp`
        как `m3u8_native`). У сайта нет выбора озвучки/плеера — один
        встроенный adaptive-манифест (hls+dash) на серию, `getSources` всегда
        отдаёт один фиктивный пункт (как у anilibria). Самое сложное во всём
        портировании источников — реверс собственной обфускации плеера, вынесен
        в отдельный пункт ниже.
  - [ ] animevost — *(добавлено по ходу работы)* `api.animevost.org` не
        отвечает ни на один запрос из этой среды разработки (curl падает без
        ответа сервера), похоже на гео/анти-бот блокировку на уровне
        Cloudflare для не-СНГ IP. Осознанно не портируется вслепую без
        возможности проверить вживую — это ограничение окружения, а не то,
        что источник признан невозможным.
  - [ ] sameband — та же причина, что и animevost: `sameband.studio` рвёт
        TLS-хендшейк (`curl -sv` → `Recv failure: Connection reset by peer`)
        для запросов из этой среды.
  - [x] *(добавлено по ходу работы)* yummy_anime_org — **убран из
        `DEFAULT_SOURCES`, портировать нечего**. В старом Qt-бэкенде
        (`old/backend/backend.py`) он импортировался как отдельный модуль
        `anicli_api.source.yummy_anime_org`, но в актуальном репозитории
        `anicli_api` на GitHub такого файла больше нет (проверено вживую —
        `anicli_api/source/` содержит только `yummy_anime.py`; сверено через
        GitHub API, а не догадкой). Судя по всему, это был отдельный
        зеркальный домен yummy_anime, который с тех пор умер или был слит с
        основным источником выше по течению, и апстрим удалил под него
        модуль. Портировать нечего — нет ни действующего домена, ни
        актуального справочника по нему.
  - [ ] *(риск, добавлено по ходу работы)* `anicli_api`/anicli-ru — внешняя
        Python-библиотека, её исходники не вендорены в этом репозитории, поэтому
        готового описания запросов/HTML-разметки под рукой нет. Портировать
        логику придётся вручную по сайту-источнику (можно свериться с исходниками
        anicli_api на GitHub как со справочником, но не копировать вслепую — сайты
        меняют вёрстку/API без предупреждения, и то же самое будет ломать и старую
        Qt-версию). Каждый источник — отдельная по объёму задача, не одна общая.
- [x] *(добавлено по ходу работы)* Разобрать получение видео из плеера Kodik
      (`kodikplayer.com` и алиасы с той же структурой ссылки — определяется
      по пути `/serial|season|video|film/<id>/<hash>/<quality>p`, тем же
      способом, что и сам `anicli_api`, а не по конкретному домену) —
      реализовано как переиспользуемый модуль
      (`electron/sources/players/kodik.ts`), не привязанный к animego.
      Разобрано не с нуля, а по актуальным исходникам `anicli_api`
      (`player/kodik.py` + `player/parsers/kodik_parser.py` на GitHub —
      источник правды, раз уж эта библиотека такое явно умеет) как
      справочнику: подписанные antihotlink-параметры страницы плеера
      (`d`/`d_sign`/`pd`/`pd_sign`/`ref`/`ref_sign`, HMAC) возвращаются на его
      же API вместе с `type`/`hash`/`id` самого видео и путём API,
      извлечённым из base64 внутри JS-бандла плеера; ответ содержит ссылки на
      m3u8, зашифрованные кастомным ROT18-шифром + base64. Каждый шаг
      перепроверен вживую реальными запросами при портировании, итоговый
      m3u8 — через `yt-dlp` (`m3u8_native`). Headless-браузер не понадобился —
      алгоритм оказался статическим (регулярки по HTML/JS), без выполнения
      кода на странице.
- [x] *(добавлено по ходу работы)* Разобрать плеер AniBoom (`aniboom.one`)
      (`electron/sources/players/aniboom.ts`) — проще Kodik: страница отдаёт
      `<video data-parameters="...">` с HTML-экранированным JSON прямо в
      атрибуте, без отдельного API-запроса и без шифрования ссылок. Разобрано
      по актуальным исходникам `anicli_api` (`player/aniboom.py`) как
      справочнику. Найденная вживую и подтверждённая на практике
      непредсказуемость (ровно то, о чём предупреждает сам `anicli_api`):
      поля `hls`/`dash` внутри `data-parameters` иногда приходят как вложенный
      JSON-объект, а иногда как JSON-строка с тем же объектом внутри —
      поймано оба варианта на одном и том же эпизоде в двух соседних запросах,
      код разбирает оба случая. Возвращается только одно качество (hls в
      приоритете, dash — запасной вариант), чтобы не плодить в списке две
      записи с одинаковым `quality: "1080"`. Для скачивания (не только для
      получения ссылки) нужны конкретные заголовки (`Referer`/`Origin`/
      `Accept-Language`), иначе CDN может вернуть 403.
- [x] *(добавлено по ходу работы)* Плееру AniBoom и плееру SibNet
      (`electron/sources/players/sibnet.ts`, нашёлся при портировании
      yummy_anime — простейший из всех: прямая ссылка на mp4 лежит прямо в
      JS-инициализации videojs на странице, без шифрования, нужен только
      `Referer`) нужны конкретные HTTP-заголовки не только чтобы получить
      ссылку на видео, но и чтобы её потом скачать. `VideoQualityInfo` и
      `DownloadJobRequest` расширены необязательным полем `headers`,
      `DownloadQueue` прокидывает их в `yt-dlp` через `--add-header`.
- [x] *(добавлено по ходу работы)* Разобрать собственный (не сторонний) плеер
      dreamerscast.com (`electron/sources/players/dreamcastChipers.ts`) —
      самый сложный из всех разобранных плееров и источников в этом проекте.
      Многослойная обфускация прямо в JS-бандле страницы: (1) JS-бандл
      запакован Dean-Edwards-style packer'ом
      (`eval(function(p,a,c,k,e,d){...}(...))`) — сначала распаковывается;
      (2) внутри лежит "crypt-код" вида `#1....==\` или `#0....==`, префикс
      определяет способ расшифровки — либо напрямую через кастомный
      base64-вариант ("Salt"), либо сперва через Caesar-подобный шифр
      ("перец/сахар"); (3) расшифрованный crypt-код — JSON-словарь с ключами
      `bk0..bk4` — это "соль", которую нужно вырезать из закодированного блока
      плейлиста страницы (`new Playerjs("...")`), прежде чем его можно
      base64-декодировать и распарсить как JSON. Разобрано по актуальным
      исходникам `anicli_api`
      (`player/dreamcast_chipers.py`, авторство оригинального декодера —
      https://github.com/barsikus007, указано прямо в его docstring) как
      справочнику, портировано максимально дословно (включая пару неочевидных
      мест вроде потери последнего символа при разборе одного из аргументов
      packer'а — это не баг порта, а точное повторение оригинального
      регэкспа). Проверено особенно тщательно: рядом с портом на TypeScript
      написан отдельный Python-скрипт, дословно копирующий исходники
      `anicli_api`, прогнан на реально скачанных `playerjs.min.js` и странице
      аниме — получен эталонный playlist (16/16 серий текущего 4 сезона
      Re:Zero), и TS-порт сверен с этим эталоном программно
      (`JSON.stringify` совпадает побайтово). Итоговая HLS-ссылка подтверждена
      как реально играбельная через `yt-dlp` (`m3u8_native`). Headless-браузер
      не понадобился — алгоритм статический (весь код выполняется на стороне
      расшифровки регулярками/битовыми операциями, без исполнения чужого JS).
- [ ] *(добавлено по ходу работы)* Реально применить SOCKS5-прокси к поиску:
      добавить `socks-proxy-agent` (или аналог) и подключить его как `dispatcher`
      к `fetch()` в источниках, когда `options.proxy.enabled` — сейчас поле есть
      в `SourceRequestOptions`, но ни один источник его не использует.

### 3. Ядро: скачивание и настройки — ✅ сделано
- [x] Реализован `DownloadQueue` (`electron/download/DownloadQueue.ts`): очередь с
      ограничением параллельности из настроек (аналог `spinBoxProcesses`), запуск
      `yt-dlp` с теми же флагами (`--no-part`, `--continue`,
      `--merge-output-format mp4` для m3u8, `--proxy`, тот же шаблон имени файла),
      события лога/завершения уходят в renderer через IPC
      (`webContents.send('download:event', …)`), `kill()` останавливает процесс.
      Проверено вживую: реальный `yt-dlp` скачал тестовый файл через всю цепочку
      IPC → main → child_process → события обратно в React.
- [x] Хранение общих настроек (`electron/appSettingsStore.ts`): путь скачивания,
      прокси, число параллельных загрузок — в `app-settings.json` в userData.
      *(Отклонение от исходного плана, добавлено по ходу работы)*: вместо
      `electron-store` использован тот же простой JSON-файл-стор, что уже был
      написан для конфига источников (`sourceConfigStore.ts`) — две одинаковые
      по сути системы (JSON-файл + get/save) не нужны, а добавлять `electron-store`
      как отдельную зависимость ради того же самого не оправдано.
- [x] Прокси: SOCKS5 поддержан для скачивания (`yt-dlp --proxy`, формат
      `socks5h://user:pass@host:port`, проверено вживую с реальным запуском
      `yt-dlp`). Для поиска прокси прокинут в `SourceRequestOptions.proxy`
      архитектурно, но пока не используется ни одним источником — все 7 всё ещё
      заглушки (раздел 2), нечем ещё проверить на реальном HTTP-запросе.
- [x] Автозагрузка `yt-dlp` при первом запуске (`electron/ytdlp/`) — скачивает
      нужный бинарник под текущую ОС (`yt-dlp.exe` / `yt-dlp_macos` / `yt-dlp_linux`)
      в userData, следует редиректам GitHub Releases. Проверено вживую: реальный
      бинарник (37 МБ) скачался при старте приложения и корректно выполнился
      (`--version`).
- [ ] *(добавлено по ходу работы)* Проверка версии `yt-dlp` — сейчас логика
      скачивает бинарник только если его нет, но никогда не обновляет и не
      сверяет версию с последним релизом. Старая Qt-версия качала актуальный
      `yt-dlp` при каждой сборке — в новой версии стоит хотя бы иногда проверять
      и предлагать обновление (сам `yt-dlp` часто чинит поломки сайтов).

### 4. UI на React — почти сделано
- [x] *(добавлено по ходу работы)* Библиотека компонентов — **Mantine**
      (`@mantine/core`, `@mantine/hooks`) + иконки `@tabler/icons-react`, вместо
      голых `<input>`/`<button>`. Каркас на `AppShell` + `Tabs` (с `keepMounted`,
      чтобы вкладки не теряли состояние при переключении — раньше это было
      сделано вручную через `display:none`, теперь через штатный API компонента).
      Тёмная/светлая тема подхватывается автоматически из системной
      (`defaultColorScheme="auto"`). Проверено вживую скриншотами каждого экрана
      в работающем Electron-приложении (доступ к записи экрана появился по ходу
      работы) — до этого визуальная проверка была недоступна.
- [x] Экран поиска (`src/AnimeSearch.tsx`) — выбор источника из списка включённых
      (тумблеры вкл/выкл настраиваются на вкладке "Источники", вместо
      `comboBoxProvider`), поле запроса, обработка состояний загрузки/ошибки.
- [x] Экран редактирования JSON-конфига источников (`src/SourcesSettings.tsx`).
- [x] Экран общих настроек (`src/AppSettingsForm.tsx`) — путь сохранения (с
      выбором папки через системный диалог), SOCKS5 (ip/port/user/pass/вкл),
      число параллельных загрузок.
- [x] Результаты поиска кликабельны — переход к списку эпизодов
      (`src/EpisodeBrowser.tsx`).
- [x] Список серий с мультивыбором (аналог `listWidgetEpisodesMany`) — клик по
      серии выбирает её, протягивание мыши с зажатой кнопкой (как в
      Finder/Explorer) выбирает диапазон в любую сторону, новый клик заменяет
      предыдущий выбор. Источник/качество подтягиваются автоматически для
      стартовой серии выбора — раньше это требовало отдельного клика по
      названию серии и (это было багом) не срабатывало вовсе, если выбирать
      только через чекбоксы; чекбоксы убраны, отдельного режима "одна серия"
      не делали — этого одного взаимодействия достаточно, не требует двух
      раздельных списков, как было в Qt.
- [x] Список источников/озвучек (`[плеер] домен`, как в текущем UI) —
      реализован, но пока проверен только на anilibria, где он вырожденный
      (всегда одна запись).
- [x] Список качеств видео — реализован, показывает `{quality}p ({type})`.
- [x] Панель загрузок (`src/DownloadsPanel.tsx`) со статусами (⏳/✔/✖), кнопкой
      остановки для активных, и live-логом выбранной загрузки (список +
      детальный лог, без отдельного окна с вкладками, как было в `LogWindow`, —
      один экран проще и того же результата достигает).
      Проверено интеграционным тестом на полном сценарии: поиск → выбор тайтла →
      эпизод → источник → качество → отметка серий → скачивание → job появляется
      во вкладке "Загрузки" (`src/App.test.tsx`, `npm run test`).
- [ ] *(добавлено по ходу работы)* Показывать версию приложения через
      `app.getVersion()` (Electron API) вместо чтения `version.txt`, как было в
      Qt-версии — нужно решить, где в UI её показывать.
- [ ] Состояния пустого/ошибочного ответа и таймаута поиска — обработка ошибок
      есть (`role="alert"` с текстом), но не проверена на реальном таймауте (не
      с чем — есть только один рабочий источник).

### 5. Проверка совместимости поведения
- [ ] Таймауты поиска и обработка ошибок процесса воспроизведены как в Qt-версии.
- [ ] Формат имени файла при скачивании совпадает с текущим
      (`{путь}/{аниме}/[{озвучка}] {номер}. {название} {качество}.{ext}`).
- [ ] Кнопка "остановить процесс" действительно убивает дочерний процесс `yt-dlp`.
- [ ] Ручной прогон всего сценария на реальных источниках: поиск → аниме → серии →
      озвучка → качество → скачивание.

### 6. Плагины источников — то, ради чего всё делается
- [ ] Документировать формат плагина источника (интерфейс, куда класть файл, пример
      минимального плагина).
- [ ] UI для добавления стороннего плагина пользователем (указать путь к файлу/папке
      плагина).
- [ ] Валидация плагина при загрузке (проверка интерфейса, предупреждение о доверии
      стороннему коду — плагин исполняет произвольный JS/TS).
- [ ] Опционально: список "известных" плагинов-источников в README/wiki.

### 7. Упаковка и дистрибуция
- [ ] Настроить сборку инсталлятора `electron-builder` под Windows/macOS/Linux
      (сейчас релиз только под Windows).
- [ ] Встраивание/автозагрузка `yt-dlp` в сборку по бинарнику на каждую ОС.
- [ ] Подпись и нотаризация сборки под macOS (иначе Gatekeeper блокирует запуск).
- [ ] Автообновление (опционально) — `electron-updater`, чего не было в Qt-версии.
- [ ] Обновить CI (`.github/workflows`) под новый стек, по возможности под
      несколько ОС.
- [ ] *(добавлено по ходу работы)* Иконка приложения: в `old/icons/` остались
      `icon.ico`/`icon.png` от Qt-версии — нужны эквиваленты для окна Electron
      (`BrowserWindow({ icon })`) и для `electron-builder` (иконки инсталлятора
      под каждую ОС).
- [ ] *(добавлено по ходу работы)* Задать production Content-Security-Policy для
      renderer — Electron в dev-режиме явно предупреждает об её отсутствии
      (`Electron Security Warning (Insecure Content-Security-Policy)`), в
      собранном приложении предупреждение пропадает, но саму политику стоит задать
      осознанно, а не полагаться на то, что предупреждение просто исчезло.

### 8. Перенос пользователей текущей версии
- [ ] Миграция настроек из `QSettings` (реестр Windows / ini на Linux/macOS) в
      `electron-store` — путь скачивания и параметры прокси, чтобы обновление было
      бесшовным.

### 9. Финал
- [ ] Обновить README под новую архитектуру (этот план — временный).
- [ ] Удалить/заархивировать Qt/Python-код после того, как новая версия покроет
      весь функционал и пройдёт ручное тестирование по сценариям из раздела 5.
